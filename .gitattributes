# Указывает, что файлы .txt должны использовать LF (Unix-формат концов строк)
*.txt text eol=lf
# Явные правила для Markdown
*.md text eol=lf
# Игнорировать изменения прав доступа для скриптов
*.sh -diff
# Указывает Git, что файлы .bin — бинарные (не пытаться diff/merge)
*.bin binary
# Автоматически нормализовать концы строк для всех текстовых файлов
* text=auto
# Указывает для больших бинарных файлов, что они должны быть закешированы в LFS
*.h5 filter=lfs diff=lfs merge=lfs -text
*.pickle filter=lfs diff=lfs merge=lfs -text
*.safetensors filter=lfs diff=lfs merge=lfs -text
*.db filter=lfs diff=lfs merge=lfs -text
